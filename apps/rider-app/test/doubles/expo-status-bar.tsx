import React from 'react';
/** RENDU-RÉEL — the status bar draws nothing this harness can see. */
export const StatusBar: React.FC<Record<string, unknown>> = (props) =>
  React.createElement('StatusBar', props as never);
